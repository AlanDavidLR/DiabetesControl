<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="tu.paquete.PERMISO_CUSTOM" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logos_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiabetesControl"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Recuperacion"
            android:exported="false" />
        <!-- Activar el BroadcastReceiver después de un reinicio -->
        <receiver
            android:name=".ui.gallery.MyRebootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver> <!-- Declarar el servicio RebootServiceClass -->
        <service
            android:name=".ui.gallery.RebootServiceClass"
            android:enabled="true"
            android:exported="true" /> <!-- Declarar el BroadcastReceiver AlarmReceiver -->
        <receiver
            android:name=".ui.gallery.AlarmReceiver"
            android:enabled="true"
            android:exported="true" /> <!-- Declarar el servicio NotificationService -->
        <service
            android:name=".ui.gallery.NotificationService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".Navegacion"
            android:exported="false"
            android:label="@string/title_activity_navegacion"
            android:theme="@style/Theme.DiabetesControl.NoActionBar" />
        <activity
            android:name=".Sign_inActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>